[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"

# ESTA ES LA SECCIÓN CLAVE PARA LAS FUNCIONES
[functions]
  directory = "netlify/functions"

# Las redirecciones que ya tenías
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200